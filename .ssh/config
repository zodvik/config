# Re-use SSH conection to same host
ControlMaster auto
# Make sure the directory exists
ControlPath   /Users/nikhil.bafna/.ssh/tmp/%h
ControlPersist 10m

# Risky
StrictHostKeyChecking no
UserKnownHostsFile /dev/null
